

#include <stdio.h>
#include <string.h>





/*
 * logBase2 - compute floor(log base 2 of x)
 *   Examples: logBase2(16) = 4, logBase2(8) = 3
 *   Assume 1 <= x <= 2^31
 *   Legal ops: ! ~ & ^ | + << >>
 *   Max ops: 90
 *   Rating: 4
 */

void echo() {
  char buf[4];
  gets(buf);
  puts(buf);
}

int main() {
  echo();
}